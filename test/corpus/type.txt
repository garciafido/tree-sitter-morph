==========================
type_declaration_statement
==========================

export type foo = Foo

---

(Program (TypeDeclarationStatement
    (Export)
    (TypeName (CamelIdentifier))
    (TypeUnion (SingleType (TypeName (PascalIdentifier))))))
