=======================
import_module_statement
=======================

import ..foo

---

(Module (statements__list (ImportStatement (ImportModuleStatement (path (Path)) (from (Identifier))))))

=====================
import_from_statement
=====================

from ...bar import Bar

---

(Module (statements__list (ImportStatement (ImportFromStatement (path (Path)) (module (Identifier)) (items__list (Identifier))))))

