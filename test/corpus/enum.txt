==========================
enum_declaration_statement
==========================

enum foo {
    a
    b
    c
}

---

(Module (statements__list (EnumDeclarationStatement (identifier) (values__list) (values__list) (values__list))))

