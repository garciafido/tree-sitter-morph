==========================
enum_declaration_statement
==========================

enum foo {
    a
    b
    c
}

---

(Module (statements__list (EnumDeclarationStatement (name (CamelIdentifier)) (values__list (CamelIdentifier)) (values__list (CamelIdentifier)) (values__list (CamelIdentifier)))))
